name: Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  Build:
    strategy:
      matrix:
        config: [debug, release]
        architecture: [x86_64]
        machine:
        - host: linux
          os: ubuntu-latest
          action: gmake2
          toolset: gcc
          extension:
        - host: linux
          os: ubuntu-latest
          action: gmake2
          toolset: clang
          extension:
        - host: windows
          os: windows-latest
          action: vs2019
          toolset: msc
          extension: .exe
    runs-on: ${{ matrix.machine.os }}
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Setup premake
      uses: abel0b/setup-premake@v2
      with:
        version: "5.0.0-beta1"
    - name: Install GCC
      if: matrix.machine.os == 'ubuntu-latest' && matrix.machine.toolset == 'gcc'
      run: sudo apt install -y gcc g++
    - name: Install Clang & LLVM
      if: matrix.machine.os == 'ubuntu-latest' && matrix.machine.toolset == 'clang'
      run: sudo apt install -y clang llvm lld
    - name: Install msbuild to PATH
      if: matrix.machine.os == 'windows-latest' && matrix.machine.toolset == 'msc'
      uses: microsoft/setup-msbuild@v1.1
    - name: Run premake
      run: premake5 ${{ matrix.machine.action }} --toolset=${{ matrix.machine.toolset }}
    - name: Build
      run: premake5 build --config=${{ matrix.config }}
    - name: Test linux
      if: matrix.machine.os == 'ubuntu-latest'
      run: bin/${{ matrix.config }}-${{ matrix.machine.host }}-${{ matrix.machine.architecture }}/Test${{ matrix.machine.extension }}